by linyu deng, at Nov, 2014

- add the _var3 method for return boolean value 09
- add the @js variable for storing the js of view component, and then load it at the end of template layout 10
- remove the js and css loaded of partial template 10.1
- integrate the common function to view_init 11 *
- update all of templates of view module for the new feature of Foundation import 11
- correct the js and css assigned 11
- merge the template layout.slim and view_layout.slim 12
- rename the user_login.slim to view_login.slim 12.0
- add the default menu `main_menu` for view_layout.slim 12.1
- update the auth.rb file 12.2
- add the level tag for some variables of user level 13.1
*
- rewrite the view_level? method for including the level estimation from argument value of _var 13
- add the tags query to tool bar of table template 15

by linyu deng, at Oct, 2014

- initialize this file by author and date, and the month moves backward for one, like Nov -> Oct
- mark down what are you want to do, and sign an asterisk at the end of each line or on the top of list block
- remove the asterisk if these tasks have been done
- the number at the end of each line is the day as task ID, and commit by like that C1411 10, C1411 10-12
  C1411 10 		- the task at Nov, 2014, id 10
  c1411 10-12 	- the task at Nov, 2014, id from 10 to 12
